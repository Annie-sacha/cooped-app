import 'package:flutter/material.dart';
class PromoteurDashboardScreen extends StatelessWidget {
  const PromoteurDashboardScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Dashboard Promoteur')));
}