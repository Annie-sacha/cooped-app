import 'package:flutter/material.dart';
class AdminPromoteursScreen extends StatelessWidget {
  const AdminPromoteursScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Promoteurs (à venir)')));
}